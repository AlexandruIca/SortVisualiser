#ifndef SORTVIS_RANDOM_HPP
#define SORTVIS_RANDOM_HPP
#pragma once

#include "event/event.hpp"

namespace core {

auto random_shuffle(std::vector<element_t>& data) -> void;

} // namespace core

#endif // !SORTVIS_RANDOM_HPP
